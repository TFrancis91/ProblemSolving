using System.Collections.Generic;

namespace graph{
    public class AdjacencyListGraph{
        /*public static void Main(){
            List<LinkedList<int>> graph=new List<LinkedList<int>>();
            for(int i=0;i<5;i++){
                graph.Add(new LinkedList<int>());
            }

            AddEdge(graph,1,2);
            

        }

        //Add edge for a undirected graph
        public static void AddEdge(List<LinkedList<int>> graph, int vertex, int adjacentVertex){
            graph[vertex].AddLast(adjacentVertex);
            graph[adjacentVertex].AddLast(vertex);
        }

        public static void RemoveEdge(List<LinkedList<int>> graph, int vertex, int adjacentVertex){
            graph[vertex].Remove(adjacentVertex);
            graph[adjacentVertex].Remove(vertex);
        }

        public static void AddVertex(List<LinkedList<int>> graph, int vertex){
            graph.Add(new LinkedList<int>());
        }

        public static void RemoveVertex(List<LinkedList<int>> graph, int vertex){
            graph.RemoveAt(vertex);
        }*/
    }
}